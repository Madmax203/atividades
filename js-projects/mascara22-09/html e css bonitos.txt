<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exercício de Validação de Formulário</title>
    <style>
        /* Estilização simples para organizar o formulário */
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        .campo {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        input {
            padding: 8px;
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .contador-senha {
            color: red;
            font-size: 0.9em;
            display: block;
            margin-top: 5px;
        }
        button {
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <h1>Formulário de Cadastro</h1>

    

        <!-- Nome: Não pode ter números -->
        <div class="campo">
            <label for="nome">Nome:</label>
            <input type="text" id="nome" name="nome" required>
        </div>

        <!-- CPF: Não pode ter letras e caracteres especiais, tamanho 14 (considerando os pontos e hífen ex: 000.000.000-00) -->
        <!-- Se o tamanho 14 exigir formatação pura em números, mude o pattern ou retire os pontos -->
        <div class="campo">
            <label for="cpf">CPF:</label>
            <input type="text" id="cpf" name="cpf" required maxlength="14" minlength="14">
        </div>

        <!-- RG: Não pode ter letras e caracteres especiais (apenas números) -->
        <div class="campo">
            <label for="rg">RG:</label>
            <input type="text" id="rg" name="rg" required>
        </div>

        <!-- CEP: Só pode ter números, tamanho 8 -->
        <div class="campo">
            <label for="cep">CEP:</label>
            <input type="text" id="cep" name="cep" required maxlength="8" minlength="8">
        </div>

        <!-- Endereço: Livre -->
        <div class="campo">
            <label for="endereco">Endereço:</label>
            <input type="text" id="endereco" name="endereco" required>
        </div>

        <!-- UF: Só pode ter letras e o tamanho é 2 -->
        <div class="campo">
            <label for="uf">UF:</label>
            <input type="text" id="uf" name="uf" required maxlength="2" minlength="2">
        </div>

        <!-- Senha: Letras e números, no mínimo 8 caracteres -->
        <div class="campo">
            <label for="senha">Senha:</label>
            <input type="password" id="senha" name="senha" required minlength="8">
            <!-- Tag preparada em vermelho para receber a contagem de caracteres futuramente -->
            <span class="contador-senha" id="contador">Caracteres digitados: 0</span>
        </div>

        <button type="submit">Enviar Cadastro</button>

    

</body>
<script src=""></script>
</html>
